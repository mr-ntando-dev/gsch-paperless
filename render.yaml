services:
  - type: web
    name: gsch-paperless
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NEXTAUTH_SECRET
        generateValue: true
      - key: NEXTAUTH_URL
        sync: false
      - key: NODE_ENV
        value: production
